# Migration Rollbacks

This directory contains rollback scripts for database migrations.

Each rollback file corresponds to a migration file and contains the SQL statements needed to undo the changes made by that migration.